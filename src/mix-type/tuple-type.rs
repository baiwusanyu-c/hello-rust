
// 和其他语言一样，你可以理解为元组，是一组由若干个任意类型的元素组成的长度已知的数组
// 它更偏向 '元'，及单元，这个概念
fn tupleDemo(){
    let tup: (i32, f64, u8) = (500, 6.4, 1);
    // 通过 . 来使用索引访问元组内容
    let five_hundred = x.0;

    let six_point_four = x.1;

    let one = x.2;
}